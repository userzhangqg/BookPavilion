# Server Configuration
PORT=8080
GIN_MODE=debug  # Use 'release' for production
UPLOAD_DIR=./uploads

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=bookpavilion
DB_PASSWORD=bookpavilion
DB_NAME=bookpavilion

# File Upload Configuration
MAX_FILE_SIZE=8388608  # 8MB in bytes
ALLOWED_FORMATS=pdf,epub,txt,mobi

# Test Database Configuration
TEST_DB_HOST=localhost
TEST_DB_PORT=3306
TEST_DB_USER=bookpavilion
TEST_DB_PASSWORD=bookpavilion
TEST_DB_NAME=bookpavilion_test

# Development Tools
DEBUG=true
LOG_LEVEL=debug  # debug, info, warn, error

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Never commit the actual .env file to version control
